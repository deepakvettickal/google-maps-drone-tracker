A simple react app to display the movement of a delivery drone between two locations.
Made with react, google maps api, and firebase.


Clone the repository, and run 
$ npm install && npm start  
to start the app.
